import { AddroleMiddlewareMiddleware } from './addrole.middleware';

describe('AddroleMiddlewareMiddleware', () => {
  it('should be defined', () => {
    expect(new AddroleMiddlewareMiddleware()).toBeDefined();
  });
});
