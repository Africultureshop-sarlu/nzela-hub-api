name: Continuous deployment

on:
  workflow_run:
    workflows: ["CI Pipeline"]
    types:
      - completed

permissions:
  contents: read

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Pull docker image
        run: sudo docker pull hobed24/nzela-hub-api:0.0.1

      - name: Delete old docker container
        run: sudo docker rm -f nzela-hub-api-container || true 

      # - name: Configure AWS credentials
      #   uses: aws-actions/configure-aws-credentials@v1
      #   with:
      #     aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     aws-region: us-west-1 # Remplacez par votre région AWS

      # - name: Run Docker Container
      #   run: sudo docker run -d -p 8080:8080 --name nzela-hub-api-container hobed24/nzela-hub-api:0.0.1

      - name: Run Docker Container
        env:
          DB_USERNAME: ${{ secrets.DB_USERNAME }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_NAME: ${{ secrets.DB_NAME }}
          DB_HOST: host.docker.internal
        run: sudo docker run -d -p 8080:8080 \
              --name nzela-hub-api-container \
              -e DB_USERNAME=$DB_USERNAME \
              -e DB_PASSWORD=$DB_PASSWORD \
              -e DB_NAME=$DB_NAME \
              -e DB_HOST=$DB_HOST \
              hobed24/nzela-hub-api:0.0.1

      # - name: Set up kubectl
      #   uses: azure/setup-kubectl@v1
      #   with:
      #     version: 'v1.18.0'

      # - name: Update kubeconfig
      #   run: |
      #     aws eks update-kubeconfig --name <your-cluster-name> --region us-west-2

      # - name: Deploy to Kubernetes
      #   run: |
      #     kubectl apply -f k8s/configmap.yaml
      #     kubectl apply -f k8s/db.yaml
      #     kubectl apply -f k8s/adminer.yaml
      #     kubectl apply -f k8s/my-app.yaml
